@model IEnumerable<OnlineResultManagementSystem.Models.tblDepartment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>@if(User.Identity.Name=="hmahmud59@gmail.com")
   { 
    @Html.ActionLink("Create New", "Create")
}
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.department_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dept_address)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.department_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dept_address)
        </td>
        <td>
            @if(User.Identity.Name=="hmahmud59@gmail.com")
            { 
            @Html.ActionLink("Edit", "Edit", new { id=item.department_id }) 
          
            @Html.ActionLink("Delete", "Delete", new { id=item.department_id })
            
            }
            @Html.ActionLink("Details", "Details", new { id = item.department_id }) |
        </td>
    </tr>
}

</table>
