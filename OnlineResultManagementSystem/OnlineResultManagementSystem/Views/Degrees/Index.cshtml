@model IEnumerable<OnlineResultManagementSystem.Models.tblDegree>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>@if(User.Identity.Name=="hmahmud59@gmail.com")
   { 
    @Html.ActionLink("Create New", "Create")
}
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.degree_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.degree_name)
        </td>
        <td>
            @if(User.Identity.Name=="hmahmud59@gmail.com")
            { 
            @Html.ActionLink("Edit", "Edit", new { id=item.degree_id }) 
          
            @Html.ActionLink("Delete", "Delete", new { id=item.degree_id })
            }
            @Html.ActionLink("Details", "Details", new { id = item.degree_id }) |
        </td>
    </tr>
}

</table>
