@model IEnumerable<OnlineResultManagementSystem.Models.tblSemester>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @if(User.Identity.Name=="hmahmud59@gmail.com")
    { 
    @Html.ActionLink("Create New", "Create")
    }
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.semester_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblDegree.degree_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblDepartment.department_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.semester_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblDegree.degree_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblDepartment.department_name)
        </td>
        <td>
            @if(User.Identity.Name=="hmahmud59@gmail.com")
            { 
            @Html.ActionLink("Edit", "Edit", new { id=item.semester_id }) 
             
            @Html.ActionLink("Delete", "Delete", new { id=item.semester_id })
            }
            @Html.ActionLink("Details", "Details", new { id=item.semester_id })
        </td>
    </tr>
}

</table>
